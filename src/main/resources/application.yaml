spring:
  application:
  name: query

  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/vdt_query
    username: longlh20
    password: 123123aA@
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 7554